# AGENTS｜社区 AI 工作台 V0.1

## 0. 总原则

### P-01｜不新增工作负担
持续理解不能成为新增工作，必须寄生在原本就会发生的社区工作里。

### P-03｜最小认知转换
不是能结构化的都结构化，只有后续业务真正要用的才结构化。

优先：
- 能保存来源，就不先归纳；
- 能排列，就不总结；
- 能引用，就不重写；
- 能用确定性操作完成，就不让模型参与认知转换。

### P-04｜运行驱动规范
先用最小规则真实运行；真实问题出现后再补规则。

### P-05｜时点输出冻结
周总结、阶段材料等生成并确认后，作为该时点历史快照保存。
后续新信息不自动改写旧版本。

---

# 1. 日常业务入口｜事项台账

日常业务输入默认只先处理到 `01_事项台账/`。

收到新信息时优先回答：

> 它改变了之前的什么？

而不是只重新描述“它自己讲了什么”。

## 来源记录
保留来源主体，例如：
- [居民]
- [工作人员]
- [物业]
- [会议]
- [部门]
- [正式文件]
- [系统产物]

不得：
- 把主体陈述自动升级为客观事实；
- 把“再次出现”自动视为同一故障；
- 自动补全过程；
- 自动制造因果；
- 自动把状态升级为“已解决/已办结”。

## MATTER-ID
- 同一工作区内已有明确同一事项时，继续原 MATTER-ID；
- 仅名称相似、语义相似，不自动合并；
- 不跨工作人员工作区自动合并。

工作人员明确形成一个新事项时，在创建 MATTER 文件前调用 `python scripts/workbench_phase1.py next-matter-id` 获取唯一 MATTER-ID，不得由模型自行推算下一个编号。

---

# 2. 今日待办

`02_今日待办/` 只接收真实形成的内部任务。

可以形成：
- 工作人员明确决定；
- 会议明确分工；
- 上级/正式文件明确交办。

不能形成：
- 居民诉求本身；
- 物业承诺；
- 部门表示；
- AI建议。

任务状态只根据明确来源变化。

---

# 3. 居民资料文件 与 居民档案

## `03_居民资料_未入档/`
这是文件整理层。
用于存放包含居民个人资料、申请资料、已填写个人表格等文件。

进入这个目录：
> 不等于建立居民档案。

## `04_居民档案/`
这是业务记录层。

只有工作人员明确选择“更新居民档案”后，才把文件中明确存在的居民事实写入对应档案。

只记录明确提供的信息。
不得自动生成“居民画像”。
同名居民不能仅凭姓名自动合并。

### 居民建档与事项关联规则

是否建立居民档案，由工作人员明确确认。

居民档案可以独立于 MATTER 存在。没有对应事项，不得为了完成居民建档而新建 MATTER。

#### 独立居民建档初始化

确认建档前，TRAE 可以根据明确来源整理拟建档内容，但不得将“建议建档”“资料完整”“拟建档内容已经生成”等理解为工作人员已经确认建档。

> 拟建档内容存在 ≠ 已确认建档。

TRAE 整理拟建档内容后必须停止，并向工作人员展示拟建档居民及拟保存路径。

只有工作人员再次明确确认“建档”后，才可以：

1. 创建或更新 `04_居民档案/` 中的居民业务档案；
2. 只写入来源明确支持的居民事实；
3. 调用：

`python scripts/workbench_phase1.py init-resident --resident-file <居民档案项目相对路径>`

由确定性工具分配或确认 RESIDENT-ID，并初始化 `## 工作台关联（系统维护）`。

`init-resident`：

- 只处理已经存在的居民档案文件；
- 不创建居民档案文件；
- 不判断是否应该建档；
- 不提取或补造居民事实；
- 不创建 MATTER；
- 当前没有事项时，`关联事项` 允许为空；
- 已初始化档案重复执行时，应沿用已有 RESIDENT-ID，不重新编号。

### 居民与事项关联

居民已经建档后，如工作人员明确确认该居民与某一 MATTER 的对应关系，再调用：

`python scripts/workbench_phase1.py link`

由确定性工具完成居民档案与事项的双向关联。

建立关联不得：

- 改变原事项状态、办理结果、来源记录或其他已有内容；
- 自动生成待办；
- 自动形成治理判断；
- 自动升级或结束事项；
- 仅凭姓名认定为同一居民；
- 因为同一居民再次出现而认定为同一事项。

后续该居民出现新信息时，应先恢复其相关历史事项，再根据新来源判断是接续已有事项还是建立新事项。

---

# 4. 填写资料模板

`05_填写资料模板/` 保存可重复使用的空白表格。

生成或填写材料时：
- 只使用明确资料；
- 未提供的信息留空；
- 不推断补全；
- 生成的 Word/表格属于工作产物，不自动成为事实来源。

---

# 5. 文件分类

新文件先放 `00_待分类/`。

固定分类仅允许：
- `06_政策资料`
- `05_填写资料模板`
- `03_居民资料_未入档`
- `07_会议材料`
- `08_工作材料`
- `09_社区知识`
- `99_无法确认`

不得自行创建其他分类。

## 首轮
AI只生成 `00_待分类/分类建议.md`，不移动。
分类阶段只给出固定分类表内的分类建议，不得提出删除、清理、合并、改写、重命名或其他文件处置建议。

## 确认后
工作人员只说“确认全部”：
> 仅归档。

工作人员确认归档后，TRAE 必须把已确认的“`00_待分类/` 源文件 → 固定目标分类目录”对应关系写入 `scripts/_runtime/` 内的 UTF-8 JSON 计划文件，并通过 `python scripts/workbench_archive.py --plan-file <项目内相对路径>` 执行；程序只负责移动和逐项报告，正式执行后自动清理本次计划文件。未确认的文件不得写入计划；出现同名冲突或单项失败时，不得报告为全部归档完成；不得另行生成 `_run_archive.py` 或其他调用脚本。

工作人员可以额外选择：
- 归档并关联/写入事项；
- 归档并更新居民档案；
- 归档并形成待办。

如果工作人员希望“纳入周总结”：
> 先把文件中的明确工作人员动作/事项结果写入事项台账；
> 周总结再从事项台账派生。

不得直接写周总结。

分类结果不是业务事实。
文件内容只有在工作人员明确确认后，才可以进入业务层。

### 文件入口：业务连接建议

文件分类不等于业务处理完成。对进入 `00_待分类/` 的文件，TRAE 在提出归档分类建议时，应同时检查该文件是否需要连接居民档案或事项台账。

业务连接只提出建议，不自动执行。工作人员确认前，不得建立居民档案、创建 MATTER 或关联已有 MATTER。

#### 居民连接建议

只能使用以下固定选项：

- 无居民连接需要
- 建议核对关联居民资料
- 建议核对是否建立居民档案
- 建议更新已有居民档案
- 无法确认

敏感资料另存，不等于不建立居民档案。

敏感资料区用于保存原始敏感证据；居民档案用于保存后续工作所需的最小居民记忆。两者不能互相替代。

如果当前文件明确指向居民或家庭，但具体姓名、联系方式、住址等身份资料位于关联名单、附件或敏感资料中，应提示“建议核对关联居民资料”，不得因此判断为“仅归档”。

是否正式建立居民档案，仍须遵守现有居民建档规则并由工作人员确认。不得为了建档补造缺失资料。

#### 事项连接建议

只能使用以下固定选项：

- 无事项连接需要
- 建议新建事项
- 建议核对是否关联已有事项
- 无法确认

同一地点、同一居民、相似问题或相近时间，不足以认定为同一 MATTER。

只有来源能够明确支持同一治理事项时，才能在工作人员确认后关联已有 MATTER；证据不足时只能提示“建议核对是否关联已有事项”。

#### 证据摘录

业务连接建议必须同时提供可核对的来源证据。

证据只能：

- 引用当前文件中的原句；
- 引用当前文件中明确出现的字段；
- 或标明当前文件无法读取。

格式例如：

- `文件中出现：“……”`
- `明确字段：“姓名：……”“联系电话：……”`
- `无法读取原文件内容，当前无可引用证据。`

不得在证据栏概括、解释、推断或使用当前文件没有的表述。

不得写成：

- “说明存在设备故障”
- “符合居民建档条件”
- “疑似存在信访风险”
- “属于持续治理事项”

#### 同一材料的多个文件版本

如果能够确认 `.doc`、`.docx`、`.pdf` 等文件属于同一份材料，应按一个“材料组”提出居民连接和事项连接建议。

多个格式文件可以共同作为同一事项或居民记录的来源，不因文件数量建立多个 MATTER。

不得因此删除、合并或改写原文件。

#### 无法读取的文件

如果当前无法实际读取 `.doc`、`.xls` 等文件内容：

- 可以根据明确的文件类型信息提出归档分类建议；
- 居民连接建议和事项连接建议应标记为“无法确认”；
- 证据摘录写“无法读取原文件内容，当前无可引用证据”。

不得使用文件名、其他材料、历史记忆或推测替代当前文件内容。

#### 当前不处理待办连接

文件入口当前不提供“建议形成待办”。

待办是否从文件入口接入，待 TODO 真实工作链得到实际运行验证后再决定。

#### 文件读取路径

处理 `00_待分类/` 文件时，不得因通用 `Read` 失败直接判断文件无法读取。

已验证的读取路径：

- `.docx`：优先使用 `pandoc` 提取正文；
- `.xlsx`：优先使用 `openpyxl` 读取表格；
- `.xls`：优先使用 `xlrd` 读取表格；
- `.pdf`：优先检查并提取文字层；若确认无文字层，标记“扫描型PDF，当前文字提取路径无法读取”，不得根据文件名补推内容；
- `.doc`：当前直接读取路径未验证通过，标记“旧DOC当前无法直接读取”，不得根据文件名补推内容。

不得仅因通用 `Read` 的 64KB 限制或二进制输出，将文件判断为无法读取。
不得为了读取单个文件临时创建 helper 或自行发明新的解析脚本。

---

# 6. 个人周总结

输出目录：`10_个人周总结/`

只保留两栏：

## 一、本周工作事项
一件事项只出现一次。

只写截至周末已经发生的：
- 工作人员动作；
- 明确结果；
- 截至周末事项状态。

不得写未来动作。

纯 [系统产物] 默认不进入个人周总结。
只有工作人员明确核对、修改、提交、发送、使用等动作后，写“人的动作”。

## 二、截至周末明确待办
只列：
- 已明确形成；
- 尚未完成；
- 属于社区内部的真实任务。

未来动作只在这一栏出现一次。

禁止额外生成：
- 工作亮点
- 群众满意
- 取得成效
- 原因分析
- 下周计划
- 第三栏

---

# 7. 对外草稿

输出目录：`11_对外草稿/`

AI不决定“报什么”。

流程：
1. 工作人员/书记人工选定要表达的事项；
2. AI只对已选事项进行必要正式化表达；
3. 人工审阅后使用。

允许：
- 调整语序；
- 书面化；
- 去掉内部编号；
- 合并同一事项内部已明确发生的动作；
- 使用“一是、二是”等结构词。

禁止：
- 新增事实；
- 改变主体；
- 改变状态；
- 把独立事项合并成不存在的系统性工作；
- 自造分类；
- 自造成效评价。

文件开头必须标注：

> 材料属性：派生材料草稿  
> 使用范围：人工审阅后用于对外表达  
> 禁止用途：不得作为事项、居民档案、待办或周总结的事实来源

---

# 8. 不做的事情

V0.1 不做：
- 多人共享工作区；
- 跨工作人员事项折叠；
- 自动居民画像；
- 自动判断事项重要性；
- 自动生成“社区周汇总”中间层；
- 任意历史时点重建；
- 自创阈值、概率、分数；
- 为了完整结构而补造信息。

---

# 9. 真实使用判断

不要把使用过程变成实验。

只看：

> 两周以后，工作人员还会不会主动打开它。

如果必须额外维护一套“给AI看的台账”，V0.1 就没有通过。
